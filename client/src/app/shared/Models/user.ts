export interface User {
    email: string
    dipslayName: string;
    token: string;
}

export interface Address {
    firstName: string
    lastName: string;
    city: string;
    state: string;
    zipcode: string;
}